/*
{
    productos.map((producto) => (
        (((producto.nombre.includes(buscar.toUpperCase()) || buscar === '')) &&
            (producto.categoria === categoria || categoria === 'Todo') &&
            (producto.precio >= min && producto.precio <= max)) &&
        <Card to={/producto/${producto.id}} key={producto.id}>
            <CardImagen>
                <Img src={producto.imagen} alt="" />
            </CardImagen>
            <CardProducto>
                <Descripcion>{producto.nombre}</Descripcion>
                <Precio>${producto.precio}</Precio>
            </CardProducto>
        </Card>
    ))
}

<Combo 
    name="categoria" 
    value={categoria}
    onChange={handleChange}>
    <option value="Todo">Todo</option>
    {
        categorias.map((cat) => (
            <option value={cat.nombre} key={cat.id}>{cat.nombre}</option>
        ))
    }
</Combo>

const handleChange = (e) =>{ //Funcion para la escritura dentro de los inputs.
        //console.log(e.target.name);
        switch (e.target.name){
            case 'buscar':
                setBuscar(e.target.value);
                break;

            case 'categoria':
                setCategoria(e.target.value);
                break;

            case 'min':
                setMin(e.target.value);
                break;

            case 'max':
                setMax(e.target.value);
                break;

            default:
                break;
        }
    }
*/